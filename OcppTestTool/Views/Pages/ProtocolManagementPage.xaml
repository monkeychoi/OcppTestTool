<Page x:Class="OcppTestTool.Views.Pages.ProtocolManagementPage"
      xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
      xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
      xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
      xmlns:local="clr-namespace:OcppTestTool.Views.Pages"
      xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
      xmlns:models="clr-namespace:OcppTestTool.Models"
      xmlns:uc="clr-namespace:OcppTestTool.UserControls"
      xmlns:ui="http://schemas.lepo.co/wpfui/2022/xaml"
      Title="ProtocolManagementPage"
      d:DataContext="{d:DesignInstance local:ProtocolManagementPage,
                                        IsDesignTimeCreatable=False}"
      d:DesignHeight="450"
      d:DesignWidth="800"
      ui:Design.Background="{DynamicResource ApplicationBackgroundBrush}"
      ui:Design.Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      Foreground="{DynamicResource TextFillColorPrimaryBrush}"
      ScrollViewer.CanContentScroll="False"
      mc:Ignorable="d">

    <Grid Margin="20">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="8" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <StackPanel Orientation="Horizontal">
            <TextBlock Text="프로토콜 관리"
                       FontSize="20"
                       FontWeight="SemiBold"
                       VerticalAlignment="Center" />
            <StackPanel Orientation="Horizontal"
                        Margin="12,0,0,0">
                <Button Content="새로고침"
                        Command="{Binding ViewModel.RefreshCommand}"
                        IsEnabled="{Binding ViewModel.IsBusy, Converter={StaticResource InverseBool}}" />
                <Button Content="신규 등록"
                        Margin="8,0,0,0"
                        Command="{Binding ViewModel.OpenCreateCommand}"
                        IsEnabled="{Binding ViewModel.IsBusy, Converter={StaticResource InverseBool}}" />
                <Button Content="수정"
                        Margin="8,0,0,0"
                        Command="{Binding ViewModel.OpenCreateCommand}"
                        IsEnabled="{Binding ViewModel.IsBusy, Converter={StaticResource InverseBool}}" />
            </StackPanel>
            
        </StackPanel>

        <Border Grid.Row="1" />
        <ScrollViewer Grid.Row="2"
                      HorizontalScrollBarVisibility="Auto"
                      VerticalScrollBarVisibility="Disabled"
                      CanContentScroll="False">

            <DataGrid ItemsSource="{Binding ViewModel.Protocols}"
                      SelectedItem="{Binding ViewModel.SelectedProtocol, Mode=TwoWay}"
                      AutoGenerateColumns="False"
                      CanUserAddRows="False"
                      CanUserDeleteRows="False"
                      IsReadOnly="True"
                      HeadersVisibility="Column"
                      GridLinesVisibility="Horizontal"
                      AlternationCount="2"
                      HorizontalGridLinesBrush="#11000000"
                      VerticalScrollBarVisibility="Auto"
                      RowHeaderWidth="0"
                      SelectionMode="Single"
                      SelectionUnit="FullRow"
                      SnapsToDevicePixels="True"
                      EnableRowVirtualization="True"
                      EnableColumnVirtualization="True"
                      HorizontalAlignment="Left"
                      Width="1100"
                      MinWidth="1100"
                      ScrollViewer.HorizontalScrollBarVisibility="Disabled"
                      ScrollViewer.CanContentScroll="False">
                <DataGrid.ColumnHeaderStyle>
                    <Style TargetType="DataGridColumnHeader">
                        <Setter Property="FontWeight"
                                Value="SemiBold" />
                        <Setter Property="Padding"
                                Value="10,6" />
                        <Setter Property="HorizontalContentAlignment"
                                Value="Left" />
                        <Setter Property="BorderBrush"
                                Value="#15000000" />
                        <Setter Property="BorderThickness"
                                Value="0,1,0,1" />
                    </Style>
                </DataGrid.ColumnHeaderStyle>
                <DataGrid.RowStyle>
                    <Style TargetType="DataGridRow">
                        <Setter Property="Padding"
                                Value="4" />
                        <Setter Property="Background"
                                Value="Transparent" />

                        <Setter Property="Template">
                            <Setter.Value>
                                <ControlTemplate TargetType="DataGridRow">
                                    <Border x:Name="Bd"
                                            Background="{TemplateBinding Background}">
                                        <DataGridCellsPresenter />
                                    </Border>
                                    <ControlTemplate.Triggers>
                                        <Trigger Property="IsMouseOver"
                                                 Value="True">
                                            <Setter TargetName="Bd"
                                                    Property="Background"
                                                    Value="#05000000" />
                                        </Trigger>
                                        <Trigger Property="IsSelected"
                                                 Value="True">
                                            <Setter TargetName="Bd"
                                                    Property="Background"
                                                    Value="#100063B1" />
                                        </Trigger>
                                        <Trigger Property="AlternationIndex"
                                                 Value="1">
                                            <Setter Property="Background"
                                                    Value="#03000000" />
                                        </Trigger>
                                    </ControlTemplate.Triggers>
                                </ControlTemplate>
                            </Setter.Value>
                        </Setter>
                    </Style>
                </DataGrid.RowStyle>

                <DataGrid.Columns>
                    <DataGridTextColumn Binding="{Binding Id}"
                                        Header="ID"
                                        Width="60" />
                    <DataGridTextColumn Binding="{Binding Name}"
                                        Header="Name"
                                        Width="120" />
                    <DataGridTextColumn Binding="{Binding Version}"
                                        Header="Version"
                                        Width="100" />
                    <DataGridTextColumn Binding="{Binding Transport}"
                                        Header="Transport"
                                        Width="120" />
                    <DataGridTextColumn Binding="{Binding Codec}"
                                        Header="Codec"
                                        Width="120" />
                    <DataGridTextColumn Binding="{Binding Meta.Description}"
                                        Header="Description"
                                        Width="250" />
                    <DataGridTextColumn Header="Created"
                                        Width="170"
                                        Binding="{Binding CreatedAt, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" />
                    <DataGridTextColumn Header="Updated"
                                        Width="170"
                                        Binding="{Binding UpdatedAt, StringFormat={}{0:yyyy-MM-dd HH:mm:ss}}" />
                </DataGrid.Columns>
            </DataGrid>
        </ScrollViewer>

        <uc:LoadingOverlay Grid.Row="0"
                           Grid.RowSpan="3"
                           Margin="-20"
                           IsOpen="{Binding ViewModel.IsBusy}"
                           Message="데이터를 불러오는 중..."
                           OverlayOpacity="0.28" />
    
        
    </Grid>
</Page>
 